Application that lists out the songs you've listened in that day and generates your day color based on the song's cover image

Takes the dominant/average color of each cover art and averages that to generate a color

- features
-- date selector excluding today
-- today's color => coming soon(maybe never) -> live update change on color based on currently-playing